# Everything under here is generated by the throwaway container at
# runtime (onboarding state, the recorder db, logs, the read-only
# custom_components mount point, ...). Only configuration.yaml is
# authored by hand and worth tracking.
config/*
!config/configuration.yaml
